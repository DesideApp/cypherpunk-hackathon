# Relay Seeds

Scripts de seeds para Relay/History se documentan en `docs/backend/relay-seeds.md`.

Ejemplo rápido:

```bash
node backend/scripts/seeds/relay.js --plan free
```

Consulta la documentación para flags, escenarios y limpieza.
